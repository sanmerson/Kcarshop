import styled from "styled-components";

export const StyledSmallProfilePic = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-whiteFixed);
  border-radius: 50%;

  font-size: 1.6rem;
  font-weight: 500;
  font-family: var(--font-inter);

  width: 3.2rem;
  height: 3.2rem;
`

export const StyledLargeProfilePic = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-whiteFixed);
  border-radius: 50%;

  font-size: 3.6rem;
  font-weight: 500;
  font-family: var(--font-inter);

  max-width: 10.4rem;
  width: 10.4rem;
  height: 10.4rem;
`